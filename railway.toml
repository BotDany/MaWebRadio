[build]
builder = "nixpacks"

[deploy]
healthcheckPath = "/api/radios"
healthcheckTimeout = 100
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

[[services]]
name = "web"

[services.variables]
NODE_ENV = "production"
PORT = "3000"