# Configuration Railway pour PostgreSQL
services:
  - type: postgres
    name: postgres
    plan: free
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=ma_webradio
    options:
      - port: 5432
